# auto-farm-configurations
Automated processes aimed at applying configurations within a farm. For example, switching a miner's pool
requires updating the miner's configuration to point to a different pool.
